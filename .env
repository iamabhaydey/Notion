PORT=5000
NODE_ENV=development

# Location where local SQLite databases are created
SQLITE_DB_DIR=./db

# Optional: Add your MongoDB Atlas connection string here to automatically switch from SQLite to MongoDB
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/?retryWrites=true&w=majority
